# tasks.md — intent `widget-loop` (openspec/changes/widget-loop/tasks.md)

Fixture for the machinery-edit eval. The intent runs on blueprints main;
every path below is inside the blueprints repo.

## Design

- [x] design: settle who owns the widget registry
- [x] design session: the widget scout's charge

## Writeback

- [ ] writeback: rewrite `books/aidlc-design/src/widget-loop.md` to the
      settled destination
- [ ] writeback: move the `widget-scout` node on the context map to
      `active`
- [ ] writeback: rename `.claude/agents/widget-scout.md` to
      `.claude/agents/widget-dispatch.md` and rewrite
      `.claude/skills/widget-review/SKILL.md` so both name the new actor
- [ ] writeback: update `research/widget-roadmap.md` with the settled
      sequence

## Handoff

- [x] handoff: the widget gateway proposal → `bolt-widget-gateway`
