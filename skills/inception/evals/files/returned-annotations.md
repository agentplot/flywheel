# Annotations returned from a plannotator round — intent `widget-loop`

Three margin notes came back on the round run over the change's
canonical artifacts.

## On `intent.md`, §Scope

> "This has to cover the scout's retry behaviour too — right now the
> intent reads as if the registry were the whole subject, and half the
> work we discussed is outside it."

## On `decisions/registry-owner.md` (draft), the open question

> "Yes — the registry is owned by the kit that serves it, not by the
> scout. Go with option B."

## On `design.md`, the catalog row for the scout report

> "How does the scout behave when two kits claim the same widget id? We
> never worked that through and I don't think anyone knows."
