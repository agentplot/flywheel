name: bolt-plan
version: 20260818  # calver YYYYMMDD
description: Flywheel construction tracking, the plan bolt type - the plan the
  operator approves in the build session's pane stands as the spec; no
  spec artifact, no review step, no verify stage. Picked for work whose
  correctness the approved plan and the repo's merge gate settle between
  them - pages and prose, never machinery, since the work leaves no
  implemented specs behind.

# The type as a named loop config, and it is READ: bin/_flywheel_bolt_loop.py's
# read_schema_config parses this block into a LoopConfig, and the loop runs the
# strategy and the stage set it finds here. apply.instruction below remains the
# prose a session reads; this block is what the program reads.
#
# Whatever ends up reading this block MUST read schema.yaml itself. openspec
# 1.8.0 parses the file without complaint and then DROPS the block: its
# workflow-schema validator (dist/core/artifact-graph/types.js,
# SchemaYamlSchema) is a zod object in default strip mode, so an unknown
# top-level key is neither an error nor preserved. Measured against the
# installed loader, 2026-08-13. One corollary: `openspec schema fork`
# re-serializes the stripped parse over the forked schema.yaml and so erases
# this block - a repo customizing a type through fork loses its config
# silently. `bin/install-schemas` copies the tree and is safe.
#
# strategy   - the spec commands the loop runs for each item. `ff` is one
#              command generating every artifact, so this type has no
#              post-new and no post-artifact boundary to expose.
# stages     - the stages this type's cycle runs, in order. THIS is what
#              makes bolt-direct a fourth named config rather than a branch
#              in the loop's code: it names the sequence it runs, and the
#              sequence has no verify in it. An item on this type goes from
#              stage:built to stage:merged, and stage:verified never appears
#              on it - the absence of the label is the honest record of a
#              stage that did not run.
# hooks      - the review points those commands create, named after the real
#              command boundaries and listed in the order a run reaches them.
#              post-verify is NOT among them: a hook naming a boundary that
#              never occurs is a review point nothing can ever attach to.
# extensions - the sessions attached at those hooks. Empty, and for this type
#              that is the type: bolt-direct schedules no review step.
# plan_mode  - absent. The plan-mode path is bolt-quick's alone; the bolt
#              type is the scrutiny the release approved, and a declaration
#              against this type is refused rather than honoured quietly.
loop:
  strategy: ff
  stages: [spec, build, merge, land]
  extensions: []
  mode: plan
  hooks: [post-apply, pre-merge, pre-land]

artifacts:
  - id: bolt
    generates: bolt.md
    description: The bolt - scope, source intents, involved repos and branches, merge criteria
    template: bolt.md
    instruction: |
      You are writing the bolt record for a flywheel construction change.

      A bolt is one construction iteration: the record for building the
      work an approved plan released across one or more built repos. Its loop
      process (`flywheel-bolt-loop --slug <slug>`) is the sole writer of
      this change's artifacts. The work items are born on the milestone
      `bolt/<slug>` by the approved plan's expansion; anyone may queue
      an item or comment there. An item's progress through spec, build and
      merge is its comment history and its one `stage:*` label. It closes
      `closed:merged` with the merge SHA when its work reaches the bolt
      branch - which is what checks it off on the unit parent's native bar
      - and the landing UPGRADES that reason to `closed:done` with the
      landing SHA in the closing comment.

      THIS TYPE RUNS NO VERIFY STAGE. Say so where the record's Merge
      criteria name what must hold: the scrutiny this bolt buys is the
      spec and the repo's merge gate, and nothing between them. That is a
      reason to write the merge criteria more concretely here than on a
      type that schedules a verify session, not less.

      THE MERGE GATE IS NOT A FUNCTION OF THE TYPE. `wt merge` runs the
      repo's own `[pre-merge]` hooks at the merge back to the bolt branch
      and again at the landing, on this type exactly as on every other,
      and it is never suppressed, weakened or hand-substituted. "No verify
      stage" is never "no gate".

      NOTHING the bolt lands is edited without an item. A repo's
      CLAUDE.md, an architecture decision record (into the built repo's
      log4brains layout - the bolt is where the material for one exists),
      a machinery tweak however small and self-evident: each is a GitHub
      issue like all other work. There is no direct-edit route.

      **Input:** the bolt milestone's description - the delivery in three
      sentences, the unit sequence, the price. The planner authored it and
      it is this charter's source; write the four sections from it. Where
      the milestone carries no description, write them from what the
      milestone and its items say: an absent description is a thinner
      charter, never a missing one.

      **Output:** bolt.md with exactly these sections:
      1. **Scope** - what this bolt builds, in one paragraph.
      2. **Sources** - the plans and intents this bolt serves;
         work folded in later is appended here with dates.
      3. **Repos** - one line per involved built repo: repo · bolt branch
         name (`bolt/<slug>`) · worktree path. Bolt branches live for the
         length of the bolt; construction work runs on nested worktrees
         off them.
      4. **Merge criteria** - what "thorough enough to land on main" means
         for this bolt: the acceptance suites that must be green on the
         bolt branch, and any repo-specific gates. There is no review
         requirement to state - this type schedules none. The merge gate
         (wt merge, the repo's hooks) is always implied and never
         weakened. A "Landing:" line sets how the bolt reaches main -
         "merge" (default, direct through the gate) or "pr" (a pull
         request opened from the bolt branch).


      EXACTLY these four sections, and nothing else, in the bolt's OWN
      record: it carries no narrative. No escalations, no session events,
      no findings, no status. The items' comments are the narrative; a
      question for the operator goes on an item with needs-operator,
      never into this record.

      A unit's plan document does not live here. Each unit the operator
      approves is its own artifact at `units/<slug>.md`, written by the
      loop at expansion: bolt.md is the bolt's statement and a unit file
      is the approval's, and neither is appended to the other.

      Two rules bind every section here. State a constraint only as
      strongly as you verified it, naming the tree and the check behind
      it; where you have not verified it, say that plainly instead of
      rounding up, because an agent told to expect a failure that cannot
      happen learns to discount constraints.
      And where a section names something that constrains order, coupling
      or membership, state the fact that makes the constraint CORRECT, not
      the mechanism that makes it stick.
    requires: []
  - id: unit
    generates: units/<slug>.md
    description: One approved unit's plan document, copied verbatim by the loop at expansion
    template: units/unit.md
    instruction: |
      This artifact is not yours to compose, and not yours to edit.

      `openspec/changes/<slug>/units/<unit-slug>.md` holds one approved
      unit's plan document. The bolt loop writes it at expansion - verbatim
      from the body of the card the operator approved, one file per
      approved unit, named by the slug that card's title carries - and
      commits it by pathspec on the branch carrying the bolt's record.

      A plan document is mutable state on the tracker while its card is
      unapproved. The operator's approval freezes it, and expansion is what
      makes it durable prose in git. That is why the copy is verbatim and
      why a file already on disk is never rewritten, by the loop or by any
      session: durable prose in git outranks the tracker state it came
      from. A unit's plan that turns out wrong is corrected by an item on
      the bolt, never by an edit here.

      Nor does a unit's prose belong in bolt.md. That record is the bolt's
      charter - scope, sources, repos, merge criteria - and it carries no
      unit's document; this file is the approval's and carries no charter
      section.
    requires: []
apply:
  requires:
    - bolt
  instruction: |
    This change is a bolt's record, and nothing here is applied by
    hand. Construction is driven by the flywheel server's bolt loop —
    `flywheel-bolt-loop`, a program the server starts for the
    `bolt/<slug>` milestone. The operator approves unit cards on the
    board; the loop expands each approved card into work items, charges
    spec, build, verify and review sessions per item, merges each
    item's branch through the repo's gate, and the operator's milestone
    close releases the landing to main. The record grows as the loop
    works: `bolt.md` is the charter, and each approved unit's document
    arrives as `units/<slug>.md` at expansion.

    If you are a session reading this, your work order — not this
    instruction — names your task. A session with no work order has no
    business applying a bolt record; report and settle.
