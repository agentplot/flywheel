# Session: chaining-and-map-home (planning, 2026-08-18)

Worked items #316 and #317 on `intent/writeback-in-session`. One
plannotator round per item, #316 first, both approved.

Outcomes (records in `../../decisions/`):

- **#316 closed two decisions**:
  `the-round-close-plan-chains-sessions` (approved with the
  supersession caveat folded) and `writeback-type-narrows-to-repair`
  (the half of #312's decision 2 that waited on the chaining design).
- **#317 closed one**: `context-map-code-lives-in-the-plugin`
  (option (a) approved).

Queued consequence assertions: #327 (round-close plan contract edits),
#328 (stale/supersede mechanic for session-defined batches), #329
(writeback-skill reframe + shared discipline reference), #330
(context-map system into the plugin).
