# Session: close-writes-destination (planning, 2026-08-18)

Worked item #312 on `intent/writeback-in-session`. Drafted three
coupled decisions in `writeback-in-session-draft.md`, ran one
plannotator round, folded the operator's four annotations.

Outcomes:

- **Closed**: the close writes the destination (amended — destination
  list open-ended, context map first-class) →
  `../../decisions/the-close-writes-the-destination.md`
- **Closed**: rounds get numbers (approved as drafted) →
  `../../decisions/rounds-get-numbers.md`
- **Open**: the standalone writeback type's fate — coupled to the
  session-chaining DX the operator sketched in the round; queued as a
  planning question on the milestone.

Queued consequences: two assertions (research worktree flip, round
number in the elaboration title), one writeback item (write the closed
decisions into the flywheel book at `agentplot/blueprints`), two
questions (session-chaining DX; context-map code location).
