# Session: naming-call

## Charge
- Change: work-object-vocabulary
- Type: interactive (lavish page, operator annotation round)
- Directory: sessions/2026-08-12-naming-call/
- Items: #13
- Goal: put the coupled work-object naming call in front of the operator
  on one page — a distinct name for the released claim, the unit-kind
  batch, and the elaboration-kind batch, no name used twice — grounded
  in #18's blast-radius inventory, costed per surface.

## Produced
- `work-object-naming.html` — the page. Four complete name triples:
  - **A — assertion / unit / elaboration** (recommended): confirm the
    tracker's vocabulary; every breaking identifier stays put; the cost
    is the largest prose pass (253 `proposal` uses move) plus two
    never-applied label renames and the two skill-directory renames.
  - **D — assertion / release / elaboration** (runner-up): A plus the
    batch-kind rename that frees the word *unit*; adds the full `bin/`
    batch-kind migration including the silent compose predicate, and
    makes the hero edit mandatory.
  - **B — proposal / unit / elaboration**: cheapest prose, disqualified
    twice over — renames the schema artifact id out from under every
    consuming repo, and promotes a word OpenSpec already owns.
  - **C — unit / release / elaboration**: the hero's word; the union of
    every other option's costs.
  Plus two riders as separate controls: the hero-copy licence (settled
  design, #12/#21) and the slash-command rename
  (`/flywheel:proposal-writing`, `/flywheel:proposal-review`).
  All counts cite #18's inventory at tree `2e707e9`
  (issue 18, comment 5270769621). Design matches `site/index.html`'s
  palette and type.

## Delivered

The operator worked the page and closed all three decisions
(annotation round of 2026-08-12; session ended by the operator):

- **The triple: Option A.** The released claim is the **assertion**;
  the construction batch kind is the **unit**; the design batch kind is
  the **elaboration**. The tracker's vocabulary wins; *proposal*
  retreats to OpenSpec's own artifact; every breaking identifier
  (schema artifact id, `assertions/**` record dirs, `--kind` values,
  the compose predicate) stays put.
- **Rider 1: hero copy licence granted.** The operator's note overrode
  the "hold for #21" default — "just fix the wording on the hero. say
  it as it is." — and confirmed the reading: the landing pass rewrites
  the two hero lines into vocabulary A ("holds the goal and its
  assertions"; the walkthrough line worded so it stops asserting the
  1:1 claim-to-bolt mapping, which closes #21's defect with it).
- **Rider 2: rename to match.** `skills/proposal-writing/` and
  `skills/proposal-review/` take the winning claim word; the slash
  commands change; 10 inbound files / 15 lines follow; the two
  never-applied `type:proposal-*` labels are renamed with them.

Outcomes and pointers posted on #13. The decisions are recorded at
`../../decisions/work-object-names.md` (the triple) and
`../../decisions/landing-pass-licence.md` (the hero-copy and
slash-command licences); the question record points to both. The
landing pass is the next work, proposed to the conductor in this
session's report.
