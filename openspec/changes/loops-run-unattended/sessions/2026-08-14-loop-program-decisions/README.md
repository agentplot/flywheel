# Session 2026-08-14 — loop-program-decisions

Planning session on `intent/loops-run-unattended`, items #81, #85,
#87, #88, #89, #161 — the six open calls left where the built loop
programs and their records disagree, plus the unit parent's missing
operator brief.

## Contents

- `round.md` — the plain-language round document the operator
  annotated: six recommendations, one per item.
- `decision-drafts.md` — the detailed drafts behind the round, with
  options, evidence pointers, and consequences.
- `plannotator-result.json` — the operator's decision: **approved**,
  all six sections "Looks good" (2026-08-16).

## Outcome

All six decisions closed on the round. Records in
`../../decisions/`: `server-filter-sweep.md` (#81),
`shared-module-home.md` (#85), `intent-scaffold-guard.md` (#87),
`intent-loop-declare-block.md` (#88), `restart-reattach.md` (#89),
`unit-parent-brief.md` (#161). The two approved doc amendments
(`design/loop-programs.md`, `bin/README.md`) landed on this session's
branch; the four builds are queued as #208–#211.

## A finding worth naming

`openspec/changes/loops-run-unattended/` did not exist before this
session — the milestone was born at triage and nothing scaffolded a
change dir. This directory tree was created by this session, and the
fact is cited as live evidence in draft 3 (#87, the scaffold guard).
