# Tasks

## 1. Label table

- [x] 1.1 Add `plan` to the label table in `bin/flywheel-setup` beside
  `unit` and `elaboration`
- [x] 1.2 Drop the "or its plan is approved" clause from
  `stage:planned`'s description

## 2. Tests

- [x] 2.1 Pin the three batch labels and the `stage:planned`
  description in the setup label test (extend or add)
- [x] 2.2 Full suite green
