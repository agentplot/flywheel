# Session 2026-08-18-docs-set — planning, item #275

Settles the operator's documentation set: which documents exist, where
each lives, and who writes into each — before any of them is drafted.
Item #275 inherits three questions from the closed
`intent/onboarding-first-run` (#182 quickstart, #180 operator tour,
#183 design book); their issue bodies are the whole inherited record.

Three decision drafts, one per document:

- `quickstart.md` — the shortest honest path from install to a first
  turn of the crank: what it assumes, the steps, where it stops, who
  maintains it.
- `operator-tour.md` — the operating half of the site's tour: what you
  type, where, what comes back; its boundary with
  `intent/site-teaches-the-system`.
- `design-book.md` — where flywheel's own design book lives and what
  writes into it; the current draft sits outside this repo.

Round: one plannotator pass over the three drafts; corrections folded
in place. Consequences each draft names become `state:queued` items on
`intent/operating-docs` once the round closes.
