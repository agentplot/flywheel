# Decision — the operator tour

Closed 2026-08-18, plannotator round on
`../sessions/2026-08-18-docs-set/operator-tour.md` (item #275;
inherits #180). No corrections were raised.

**The operator tour is a site section — the operating half of the
site's tour — built from a real run of the quickstart path**: actual
prompts, the actual tracker items and board rows that resulted,
rendered as transcript-and-result pairs, never hand-written narrative.

Boundary with the sibling: `intent/operating-docs` owns the tour's
content; `intent/site-teaches-the-system` owns the page frame both
tours (concept + operating) sit in. Neither blocks the other's
drafting.

Born from a `type:interactive` mockup session the operator annotates;
maintained by the same rule as the quickstart — a change that alters
what the operator types or sees updates the affected pair in the same
change. Full rationale in the session draft.
